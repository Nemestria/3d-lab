import LabPlaceholder from '@/components/ui/LabPlaceholder'

// Landing target for lab projects that haven't been built yet. The "SOON" cards
// on the index link here. Uses the default (WIP) copy.
export default function WipPage() {
  return <LabPlaceholder />
}
